---
title: Current Event Staff
tags: []
---

-# It's less efficient to loop over them all and ignore them
-# but it keeps rest identical between Current and Historical
- data.staff.each do |year|
  - next unless year[0] == data.event.year
  %h3= "Frostburn #{year[0]}"
  - year[1..-1].each do |group|
    - group.each_pair do |k,v|
      %p
        %strong= k
      - v.each_pair do |p,n|
        %strong= "#{p}:"
        #{n}</br>
